# Optimal dorsal hippocampal Neuropixels stack — all allowlisted cell types.
# Designed for decoder / figure runs that need CA1–CA3–DG plus Sub/MEC afferents.
# Depth bands follow the schematic HPC geometry; stereotaxic coords are a
# plausible dorsal hippocampal targeting (not histology-confirmed).
#
#   python run_simulation.py \
#     --output outputs/hpc_optimal_001 \
#     --trajectory hpc_optimal \
#     --seed 1

animal:
  strain: C57_WT
  species: mouse
  atlas: Allen_CCF
  hemisphere: right
  hemisphere_note: "Inferred from positive ML."
  coordinate_status: approximate_optimal_stack_not_histology_confirmed

probe:
  type: NP2.0
  shank_count: 1
  selected_shank: 1
  site_pitch_um: 15
  site_pitch_note: "Typical NP2.0 value; confirm from channel map."
  confirm_from_channel_map: true
  n_channels_recorded: null
  active_channel_start: null
  active_channel_end: null
  channel_map_file: null
  n_channels: 384

insertion:
  bregma_to_lambda_mm: 3.8
  # Dorsal hippocampus targeting (more medial than lab_npx2_default Sub/ENT).
  ap_mm_from_bregma: -2.0
  ml_mm_from_bregma: 1.75
  dv_mm_from_brain_surface: 2.6
  dv_uncertain: true
  horizontal_angle_deg: 0
  vertical_angle_deg: 90
  angle_convention_uncertain: true
  coordinate_source: schematic_optimal_hippocampal_stack
  trajectory_export_file: null
  histology_registered_file: null
  notes: >-
    Synthetic optimal dorsal HPC insertion: CA1→CA2→CA3→DG→Subiculum→MEC along
    depth so every RatInABox allowlisted cell type is capturable. Coordinates are
    anatomically motivated but not histology-registered.

simulation:
  use_trajectory_regions: true
  anatomy_regions_file: configs/trajectories/hpc_optimal_regions.csv
  cell_capture_file: configs/trajectories/hpc_optimal_cell_capture.yaml
  fallback_to_schematic_hippocampus: false
  exclude_non_hippocampal_regions_by_default: true

decoder:
  deployment_spike_source: sorted
  allow_ground_truth_diagnostics: false
  use_ground_truth_for_model_selection: false
